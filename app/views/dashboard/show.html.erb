<div class="container p-4 mx-auto w-full flex flex-row justify-evenly">
  <div>
    <%= render "calendars/calendar" %>
  </div>
  <% if account_admin?(current_account, current_user) %>
    <div>
      <h1><%= render "dashboard/daily_data" %></h1>
    <div>
  <% end %>
</div>
