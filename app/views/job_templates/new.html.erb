<%= content_for :title, t("shared.scaffold.new.title", model: "Job Template") %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Job Types", root_path, class: "text-black" %>
      <span class="mx-2 font-light text-gray-400">\</span>
      <%= t("shared.scaffold.new.title", model: "Job Type") %>
    </h1>
  </div>
  <div class="p-8 bg-white rounded shadow">
    <%= render "form", job_template: @job_template %>
  </div>
</div>
