<% if embed.thumbnail_url %>
  <div class="embed">
    <%= image_tag embed.thumbnail_url %>
  </div>

<% elsif embed.html %>
  <div class="embed">
    <%= sanitize embed.html %>
  </div>

<% elsif embed.provider_name %>
  <%= link_to embed.provider_name, oembed.url %>
<% end %>
