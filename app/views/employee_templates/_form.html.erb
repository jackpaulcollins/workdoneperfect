<%= form_with model: employee_template, data: { controller: 'nested-form', nested_form_wrapper_selector_value: '.nested-form-wrapper' } do |form| %>
  <%= render "shared/error_messages", resource: form.object %>

  <div class="form-group">
    <%= form.label :template_title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>


  <template data-nested-form-target="template">
    <%= form.fields_for :employee_attributes, EmployeeAttribute.new, child_index: 'NEW_RECORD' do |fields| %>
      <%= render "employee_attributes/employee_attribute_form", f: fields %>
    <% end %>
  </template>



  <!-- Inserted elements will be injected before that target. -->
  <div data-nested-form-target="target"></div>

  <button type="button" data-action="nested-form#add">Add Attribute</button>

  <%= form.button button_text(form.send(:submit_default_value)), class: "btn btn-primary" %>

  <% if form.object.new_record? %>
    <%= link_to t("cancel"), employee_templates_path, class: "btn btn-white" %>
  <% else %>
    <%= link_to t("cancel"), employee_template_path(@employee_template), class: "btn btn-white" %>
  <% end %>
  </div>
<% end %>
